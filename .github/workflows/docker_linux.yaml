on: [push]

jobs:
  linux_job:
    runs-on: ubuntu-latest
    name: Test
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Test
        uses: ./Dockerfile_qgc-build-linux
        id: test